{
  "name": "sanctuary-social",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @deoly/api & npm run dev --workspace @sanctuary/web",
    "dev:api": "npm run dev --workspace @deoly/api",
    "dev:web": "npm run dev --workspace @sanctuary/web",
    "dev:mobile": "npm run dev --workspace @deoly/mobile",
    "build": "npm run build --workspace @sanctuary/shared && npm run build --workspace @deoly/api && npm run build --workspace @sanctuary/web",
    "test": "npm run test --workspace @deoly/api",
    "db:generate": "npm run prisma:generate --workspace @deoly/api",
    "db:migrate": "npm run prisma:migrate --workspace @deoly/api",
    "db:seed": "npm run prisma:seed --workspace @deoly/api"
  }
}
